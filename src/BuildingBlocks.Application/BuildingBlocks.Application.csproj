<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\BuildingBlocks.Domain\BuildingBlocks.Domain.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <PropertyGroup>
    <Version>0.2.0</Version>
    <PackageId>aalmuqahwi.BuildingBlocks.Application</PackageId>
    <Description>CQRS and cross-cutting contracts — ICommand, IQuery, IDispatcher, IPipelineBehavior, IUnitOfWork, IIntegrationEvent, IOutboxWriter, IOutboxMessageStore, IOutboxProcessor. Depends on BuildingBlocks.Domain.</Description>
    <PackageTags>ddd;cqrs;clean-architecture;building-blocks</PackageTags>
  </PropertyGroup>

</Project>
